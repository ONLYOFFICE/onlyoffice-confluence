name: E2E

on:
  workflow_dispatch:
    inputs:
      confluence_version:
        description: 'atlassian/confluence image tag (empty — "max" from atlassian-plugin-marketing.xml)'
        default: ''
      documentserver_image:
        description: 'Document Server image'
        default: 'onlyoffice/documentserver:latest'
  push:
    branches: [master]
    paths-ignore:
      - '**/*.md'
      - 'LICENSE'
      - '.gitignore'
      - '.github/**'
      - '.check-licenses.yml'
  pull_request:
    branches: [master, develop]
    paths-ignore:
      - '**/*.md'
      - 'LICENSE'
      - '.gitignore'
      - '.github/**'
      - '.check-licenses.yml'

permissions:
  contents: read

concurrency:
  group: e2e-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      confluence_version: ${{ steps.version.outputs.value }}
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v7

      - name: Resolve Confluence version
        id: version
        env:
          VERSION_INPUT: ${{ inputs.confluence_version }}
        run: |
          version="$VERSION_INPUT"
          if [ -z "$version" ]; then
            version=$(sed -n 's/.*<product name="confluence"[^>]*max="\([^"]*\)".*/\1/p' \
              src/main/resources/atlassian-plugin-marketing.xml | head -n 1)
          fi
          if [ -z "$version" ]; then
            echo '::error file=src/main/resources/atlassian-plugin-marketing.xml::Could not read the "max" Confluence version'
            exit 1
          fi
          echo "value=$version" >> "$GITHUB_OUTPUT"
          echo "Confluence version: $version"

      - name: Install Java 21
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: Build Artifact
        run: |
          cd ${{ github.workspace }}
          cwd=$(pwd)
          sudo sh -c 'echo "deb https://packages.atlassian.com/debian/atlassian-sdk-deb/ stable contrib" >>/etc/apt/sources.list'
          wget https://packages.atlassian.com/api/gpg/key/public
          sudo apt-key add public
          sudo apt-get update -y
          sudo apt-get install atlassian-plugin-sdk -y
          atlas-version
          atlas-package

      - name: Upload plugin
        uses: christopherHX/gitea-upload-artifact@v4
        with:
          name: plugin
          path: target/onlyoffice-confluence-plugin-*.jar
          retention-days: 1
          if-no-files-found: error

  e2e:
    needs: build
    uses: ONLYOFFICE/integration-tests/.github/workflows/e2e.yml@develop
    with:
      system: confluence
      artifact_name: plugin
      documentserver_image: ${{ inputs.documentserver_image || 'onlyoffice/documentserver:latest' }}
      env_overrides: CONFLUENCE_VERSION=${{ needs.build.outputs.confluence_version }}
